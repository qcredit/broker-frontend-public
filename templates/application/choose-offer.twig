{% extends 'base.twig' %}
{% block title %}Confirm offer{% endblock %}
{% block container %}
  <div class="container mt-3">
    <div class="row">
      <div class="col">
        <p>Please make your final selections for offer #{{ offer.id }}</p>
      </div>
    </div>
    <div class="row">
      {% if offer.errors %}
        <ul>
        {% for error in errors %}
          <li>{{ error }}</li>
        {% endfor %}
        </ul>
      {% endif %}
      <form method="POST">
        <div class="form-group">
          <label for="signingMethod">Signing method:</label>
          <select id="signingMethod" name="signingMethod">
            <option value="ema">Ema</option>
            <option value="lendur">Lendur</option>
          </select>
        </div>
        <div class="form-group">
          <button type="submit" class="btn btn-primary">Let's go!</button>
        </div>
      </form>
    </div>
  </div>
{% endblock %}